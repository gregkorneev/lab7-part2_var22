## **Поиск НОД двух очень больших чисел**
---

## 🔧 **Команда для полной пересборки и запуска проекта**

```bash
rm -rf build && cmake -B build && cmake --build build && ./build/bin/app && python3 plot_gcd.py
```

---

# 📌 Описание работы

В этой лабораторной работе реализовано сравнение двух подходов для вычисления НОД (наибольшего общего делителя) двух **очень больших** чисел:

### **1. Основной метод — алгоритм Евклида (O(log(min(a, b))))**

* Быстрый, эффективный, выдерживает большие значения (10⁶–10¹²).
* Использует операцию деления по модулю.
* Подходит для высокопроизводительных вычислительных экспериментов.

### **2. Альтернативный метод — наивный метод (грубая сила)**

* Реализован через многократное вычитание.
* Экспоненциально медленный для больших чисел.
* Используется только для сравнения на малых входных данных.

---

# 📊 Эксперимент

Программа:

1. Генерирует случайные пары чисел разного диапазона.
2. Запускает оба алгоритма на малых значениях.
3. Запускает только Евклид на больших значениях.
4. Измеряет время работы и сохраняет результаты в:

```
data/timings_gcd.csv
```

Скрипт `plot_gcd.py` строит график зависимости времени выполнения от размера чисел и сохраняет его в:

```
data/gcd_time.png
```

---

# 📁 Структура проекта

```
lab7-part2_var22/
├─ CMakeLists.txt
├─ README.md
├─ plot_gcd.py
├─ data/
│  └─ (CSV и графики)
└─ src/
   ├─ main.cpp
   ├─ include.h
   ├─ gcd_euclid.cpp
   ├─ gcd_naive.cpp
   └─ utils.cpp
```

---

# 📌 Описание каждого файла

## **CMakeLists.txt**

Сборочная конфигурация проекта:

* подключает все `.cpp` файлы
* задаёт стандарт C++17
* задаёт путь вывода бинарных файлов

## **src/main.cpp**

Главный файл программы:

* генерирует тестовые данные разных диапазонов
* запускает оба алгоритма
* проводит измерения времени
* сохраняет результаты в CSV
* выводит демонстрационный пример НОД на больших числах

## **src/include.h**

Единственный заголовочный файл:

* структура `GcdPair`
* прототипы всех функций
* объявления методов Евклида, наивного метода и функций измерения времени

## **src/gcd_euclid.cpp**

Реализация **основного** метода — алгоритма Евклида.
Быстрый, работает за логарифмическое время.

## **src/gcd_naive.cpp**

Реализация **наивного** метода НОД (многократное вычитание).
Используется только на малых входах для сравнения.

## **src/utils.cpp**

Вспомогательные функции:

* генерация случайных чисел
* замер времени выполнения каждого метода
* вспомогательные структуры

## **plot_gcd.py**

Строит график зависимости:

* времени работы алгоритма Евклида
* времени работы наивного метода

и сохраняет его в `data/gcd_time.png`.

## **data/**

Папка, содержащая:

* `timings_gcd.csv` — результаты эксперимента
* `gcd_time.png` — график
* другие артефакты экспериментов

---

# 🧾 Вывод

Проект демонстрирует различия в скорости двух подходов к вычислению НОД:

* **алгоритм Евклида** показывает логарифмическую скорость и эффективен даже на больших числах;
* **наивный метод** подтверждает свою непрактичность и используется только как сравнительный эталон.
